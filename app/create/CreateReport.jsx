import { View, Text } from "react-native";

export default function CreateReport() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>👮 מסך יצירת דיווח (CreateReport)</Text>
    </View>
  );
}